//var startButton = document.getElementById("btn-start");
//var stopButton = document.getElementById("btn-stop");
/*
let counter = 0;
let timeout;
let timerOn = 0;

function timedCount() {
    document.getElementById("myTime").value = counter;
    counter ++;
    timeout = setTimeout(timedCount,1000);
}

function start() {
    if (! timerOn) {
        timerOn = 1;
        timedCount();
    }
}

function stop() {
    clearTimeout(timeout);
    timemerOn = 0;
}
*/


